<%= form_for current_user do |f| %>
  <p><%= f.label :email %></p>
  <p><%= f.text_field :email, placeholder: current_user.email %></p>
  <p><%= f.label :password %> (min. five characters, max ten characters)</p>
  <p><%= f.password_field :password %></p>
  <%= f.submit %>
<% end %>
